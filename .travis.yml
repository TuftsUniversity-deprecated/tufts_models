language: ruby
rvm:
  - 2.1

notifications:
  email:
    - justin@curationexperts.com

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true

bundler_args: --without development debug

services:
  - redis-server
